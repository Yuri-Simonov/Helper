import { IInfo } from '@types';

import { PROTRACTOR } from './information/protractor';

export const testingInfo: IInfo[] = [
    { chapter: 'Общие понятия' },
    PROTRACTOR,
    // { chapter: 'Прочее' },
];
